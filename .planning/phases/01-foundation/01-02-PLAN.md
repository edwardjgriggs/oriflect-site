---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/ThemeToggle.astro
  - src/components/Logo.astro
  - src/layouts/BaseLayout.astro
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
must_haves:
  truths:
    - "Desktop viewport shows horizontal nav links (Home, Services, About, Blog) and an inline CTA button"
    - "Mobile viewport hides nav links and shows a hamburger icon that opens a menu panel"
    - "A 'Book a Discovery Call' CTA button is visible on every page without opening the mobile menu"
    - "Footer displays email, phone, location, and LinkedIn/Twitter social links"
    - "Navigation bar stays fixed at the top when scrolling"
    - "Theme toggle switches between light and dark mode from the nav bar"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Sticky navigation with desktop links, mobile hamburger, CTA button, theme toggle"
      min_lines: 40
    - path: "src/components/Footer.astro"
      provides: "Contact info (email, phone, location) and social links (LinkedIn, Twitter/X)"
      min_lines: 20
    - path: "src/components/ThemeToggle.astro"
      provides: "Dark/light mode toggle button with localStorage persistence"
      min_lines: 10
    - path: "src/components/Logo.astro"
      provides: "Logo component (text placeholder until real logo provided)"
      min_lines: 3
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Header.astro"
      via: "import and render in body"
      pattern: "import Header from.*Header\\.astro"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Footer.astro"
      via: "import and render in body"
      pattern: "import Footer from.*Footer\\.astro"
    - from: "src/components/Header.astro"
      to: "src/components/ThemeToggle.astro"
      via: "import and render in nav"
      pattern: "import ThemeToggle from.*ThemeToggle\\.astro"
    - from: "src/components/Header.astro"
      to: "src/components/Logo.astro"
      via: "import and render as nav brand"
      pattern: "import Logo from.*Logo\\.astro"
user_setup:
  - service: vercel
    why: "Static site hosting and deployment"
    env_vars: []
    dashboard_config:
      - task: "Connect GitHub repo to Vercel for automatic deploys"
        location: "Vercel Dashboard -> New Project -> Import Git Repository"
      - task: "Add oriflect.com custom domain"
        location: "Vercel Dashboard -> Project Settings -> Domains"
---

<objective>
Build the Header (sticky nav, hamburger menu, CTA, theme toggle), Footer (contact info, social links), and wire them into BaseLayout. Deploy to Vercel.

Purpose: Delivers the visible, interactive navigation shell that all pages share -- the first thing visitors see and use. Completes NAV-01 (responsive nav), NAV-02 (persistent CTA), and NAV-03 (footer).
Output: Fully responsive site shell with working navigation, deployed to a preview URL.
</objective>

<execution_context>
@C:/Users/edwar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/edwar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - BaseLayout structure the components must integrate with -->

From src/layouts/BaseLayout.astro (created in Plan 01):
- Imports global.css and all @fontsource-variable packages
- Props: { title: string; description?: string }
- Body has classes: bg-ivory text-charcoal font-body dark:bg-dark-bg dark:text-dark-text
- Dark mode FOUC prevention script in head
- Currently has main with slot -- Header and Footer need to be added around it

From src/styles/global.css (created in Plan 01):
- @theme defines: --color-sapphire, --color-gold, --color-ember, --color-charcoal, --color-ivory
- @theme defines: --color-sapphire-tint, --color-gold-tint
- @theme defines: --color-dark-bg, --color-dark-surface, --color-dark-text
- @theme defines: --font-heading, --font-body, --font-accent, --font-code
- Dark mode via: @custom-variant dark

From src/assets/logo.svg (created in Plan 01):
- Text-based placeholder logo, white text, sized for nav bar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Header, Footer, ThemeToggle, and Logo components</name>
  <files>src/components/Header.astro, src/components/Footer.astro, src/components/ThemeToggle.astro, src/components/Logo.astro</files>
  <action>
1. Create `src/components/Logo.astro`:
- Render the logo SVG from `src/assets/logo.svg` using an Astro Image import, or inline the SVG directly
- Wrap in an `<a href="/">` link
- Alt text: "Oriflect - AI Consulting"

2. Create `src/components/ThemeToggle.astro`:
- A button with `aria-label="Toggle dark mode"`
- Show a sun icon when in dark mode (clicking switches to light), moon icon when in light mode (clicking switches to dark)
- Use simple SVG icons (inline, no icon library needed) -- small sun/moon outlines
- Script: toggle 'dark' class on document.documentElement, persist to localStorage
- Style: `p-2 rounded-md hover:bg-white/10 transition-colors` (subtle on the dark sapphire header)

3. Create `src/components/Header.astro`:
- Import ThemeToggle and Logo
- Outer header with: `sticky top-0 z-50 bg-sapphire text-white`
- Inner nav with: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-between h-16`
- Left: Logo component
- Center/Right (desktop, `hidden md:flex items-center gap-8`):
  - Nav links: Home (/), Services (/services), About (/about), Blog (/blog)
  - Link styles: `hover:text-gold transition-colors font-body font-medium`
  - CTA button: "Book a Discovery Call" -- use Ember Orange (`bg-ember hover:bg-ember/90 text-white font-body font-medium px-5 py-2.5 rounded-md transition-colors`)
  - ThemeToggle component
- Right (mobile, `flex md:hidden items-center gap-3`):
  - Compact CTA: "Book a Call" -- same ember styling but smaller (`text-sm px-3 py-1.5`). This ensures NAV-02 (CTA visible without opening menu).
  - Hamburger button: SVG three-line icon, `aria-label="Open menu"`, `p-2`
- Mobile menu panel (below nav, `hidden md:hidden`):
  - `id="mobile-menu"`, `bg-sapphire border-t border-white/10 px-4 pb-4`
  - Same four nav links as desktop, styled as `block py-3 hover:text-gold transition-colors`
  - ThemeToggle at the bottom of the mobile menu
- Script: toggle 'hidden' class on mobile-menu when hamburger is clicked. Also toggle the hamburger icon between three-lines and X.

4. Create `src/components/Footer.astro`:
- Background: Deep Sapphire (`bg-sapphire text-white`) -- matching the header creates visual consistency.
- Inner container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12`
- Layout: Use a responsive grid -- on desktop 3 columns, on mobile stacked.
  - Column 1: Brand -- Logo text "Oriflect" + short tagline ("AI consulting for growing businesses")
  - Column 2: Contact -- email (placeholder: hello@oriflect.com), phone (placeholder: (555) 123-4567), location (placeholder: Remote / Nationwide)
  - Column 3: Social -- LinkedIn and Twitter/X links (use # as placeholder hrefs). Use inline SVG icons for LinkedIn and Twitter/X logos.
- Bottom bar: copyright line "2026 Oriflect. All rights reserved." with `border-t border-white/10 mt-8 pt-8 text-sm text-white/60`
- No nav links or legal links per user decision (v1).

All components must support dark mode. Since Header and Footer already use bg-sapphire (dark background), they look fine in both modes. The key dark mode transitions happen in the page content area (handled by BaseLayout).
  </action>
  <verify>
    <automated>cd C:/Users/edwar/Desktop/Claude/test && npx astro build 2>&1 | tail -5; echo "---"; node -e "const fs=require('fs'); const h=fs.readFileSync('dist/index.html','utf8'); console.log('header:', h.includes('sticky')); console.log('footer:', h.includes('hello@oriflect.com')); console.log('cta:', h.includes('Book a Discovery Call')); console.log('mobile-cta:', h.includes('Book a Call'));"</automated>
  </verify>
  <done>Header renders with sticky positioning, desktop nav links, CTA button (Ember Orange), mobile hamburger with compact CTA, and theme toggle. Footer renders with contact info, social links, and copyright. All components use brand design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Wire components into BaseLayout and remove temporary toggle</name>
  <files>src/layouts/BaseLayout.astro, src/pages/index.astro</files>
  <action>
1. Update `src/layouts/BaseLayout.astro`:
- Add imports for Header and Footer in the frontmatter
- Place Header component before main
- Place Footer component after main
- Ensure main has appropriate classes for content spacing below the sticky header (since header is sticky with h-16, it stays in flow so no padding-top hack is needed -- but verify and add pt-16 if the header overlaps content)

2. Update `src/pages/index.astro`:
- Remove the temporary inline theme toggle that was added in Plan 01 for testing
- Keep the design system showcase content (colors, fonts) as it serves as a visual verification aid
- Ensure the page content has generous top padding since the header is now present

3. Verify all four pages (/, /services, /about, /blog) render with Header and Footer via the shared layout.

4. Run `npx astro build` and verify zero errors. Check the built HTML to confirm header and footer are present on all pages.
  </action>
  <verify>
    <automated>cd C:/Users/edwar/Desktop/Claude/test && npx astro build 2>&1 | tail -3; echo "---"; for f in dist/index.html dist/services/index.html dist/about/index.html dist/blog/index.html; do echo "$f:"; node -e "const h=require('fs').readFileSync('$f','utf8'); console.log('  header:', h.includes('menu-toggle')); console.log('  footer:', h.includes('oriflect.com'));"; done</automated>
  </verify>
  <done>All four pages render with shared Header and Footer via BaseLayout. No temporary test elements remain. Static build succeeds with zero JS bundles (only inline scripts for dark mode and menu toggle).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of responsive site shell</name>
  <files>n/a</files>
  <action>
Present the completed site shell for human review. Start the dev server with `npm run dev` and provide instructions for verification. No code changes in this task -- purely verification.
  </action>
  <verify>
    <automated>cd C:/Users/edwar/Desktop/Claude/test && npx astro build 2>&1 | tail -3</automated>
  </verify>
  <done>Human has reviewed and approved the responsive site shell including header, footer, navigation, CTA, hamburger menu, and dark mode toggle.</done>
  <what-built>Complete responsive site shell: sticky header with nav links, hamburger menu, CTA button, dark mode toggle, footer with contact info and social links. All four pages share the layout.</what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:4321
2. Desktop (wider than 768px):
   - Header is sticky at top with Deep Sapphire background
   - Nav links (Home, Services, About, Blog) are visible
   - "Book a Discovery Call" button is Ember Orange
   - Theme toggle icon is in the nav bar
3. Mobile (narrower than 768px, use browser dev tools to resize):
   - Nav links are hidden, hamburger icon is visible
   - "Book a Call" compact CTA is visible WITHOUT opening the menu
   - Tapping hamburger opens a menu panel with all nav links
   - Tapping again closes the menu
4. Dark mode:
   - Click theme toggle -- page content area switches to dark background
   - Refresh the page -- dark mode persists (no flash of white)
5. Footer (scroll to bottom):
   - Shows email, phone, location
   - Shows LinkedIn and Twitter/X icons
   - Copyright line at bottom
6. Navigate to /services, /about, /blog -- all have same header/footer
7. Brand colors: sapphire header, ember CTA, ivory page background, charcoal text
  </how-to-verify>
  <resume-signal>Type "approved" if the shell looks professional and functions correctly, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All four pages render with Header (sticky nav, hamburger, CTA) and Footer (contact, social)
2. Desktop: horizontal nav links + inline CTA + theme toggle
3. Mobile: hidden nav + visible compact CTA + hamburger that opens/closes menu panel
4. Dark mode works on all pages, persists across refresh, no FOUC
5. Footer shows contact info (email, phone, location) and social links (LinkedIn, Twitter/X)
6. `npx astro build` succeeds with static HTML output
7. No render-blocking JavaScript (SEO-04: fast load times)
</verification>

<success_criteria>
- NAV-01: Desktop shows horizontal nav, mobile shows hamburger with collapsible menu
- NAV-02: "Book a Discovery Call" CTA visible on every page on every viewport (compact version on mobile without opening menu)
- NAV-03: Footer shows contact info and social links (no legal links per user decision)
- Site builds to static HTML with minimal JavaScript (only inline scripts for dark mode + menu toggle)
- Human approves the visual design and responsive behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
