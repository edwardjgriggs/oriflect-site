---
phase: 03-conversion-mechanisms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/contact.astro
  - src/layouts/BaseLayout.astro
  - .env
  - .env.example
autonomous: true
requirements:
  - CONT-01
  - CONT-02

must_haves:
  truths:
    - "Contact page displays a form with name, email, company, and message fields"
    - "Form validates on blur with red border and inline error messages"
    - "Submit button is disabled until all fields are valid"
    - "Form submits to Web3Forms and shows inline success message (no redirect)"
    - "Inline Calendly embed renders on the right side of the contact page"
    - "Contact page has FAQ section with 2-3 questions"
    - "Contact details (email, phone) and response time promise are visible"
  artifacts:
    - path: "src/pages/contact.astro"
      provides: "Contact page with form, Calendly embed, FAQ, contact details"
      min_lines: 150
    - path: "src/layouts/BaseLayout.astro"
      provides: "Calendly widget.js and CSS loaded sitewide"
      contains: "calendly.com/assets/external/widget"
    - path: ".env"
      provides: "Environment variables for Web3Forms and Calendly"
      contains: "PUBLIC_WEB3FORMS_KEY"
    - path: ".env.example"
      provides: "Template for environment variables"
      contains: "PUBLIC_CALENDLY_URL"
  key_links:
    - from: "src/pages/contact.astro"
      to: "https://api.web3forms.com/submit"
      via: "fetch POST in form submit handler"
      pattern: "fetch.*web3forms"
    - from: "src/pages/contact.astro"
      to: "Calendly.initInlineWidget"
      via: "inline script on page load"
      pattern: "Calendly\\.initInlineWidget"
    - from: "src/layouts/BaseLayout.astro"
      to: "Calendly widget.js"
      via: "script tag in head"
      pattern: "assets\\.calendly\\.com"

user_setup:
  - service: web3forms
    why: "Form submission backend (free, no server needed)"
    env_vars:
      - name: PUBLIC_WEB3FORMS_KEY
        source: "https://web3forms.com -> Create Access Key (instant, free)"
  - service: calendly
    why: "Discovery call scheduling"
    env_vars:
      - name: PUBLIC_CALENDLY_URL
        source: "Calendly Dashboard -> Event Type -> Copy link (e.g. https://calendly.com/yourname/discovery-call)"
---

<objective>
Build the contact page with Web3Forms-powered contact form and inline Calendly scheduling embed, plus load Calendly assets sitewide in BaseLayout.

Purpose: Give visitors two conversion paths -- send a written inquiry or book a discovery call directly -- from a single page.
Output: contact.astro page, .env configuration, Calendly script loaded in BaseLayout head.
</objective>

<execution_context>
@C:/Users/edwar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/edwar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conversion-mechanisms/03-CONTEXT.md
@.planning/phases/03-conversion-mechanisms/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
  description?: string;
}
```
BaseLayout wraps content in Header + main + Footer. Calendly widget.css and widget.js must be added to the <head> section (after the favicon link, before the dark mode script).

From src/components/Header.astro:
- Nav links pattern: `<a href="/page" class="hover:text-gold transition-colors font-body font-medium">Page</a>`
- CTA button pattern: `<a href="#book" class="bg-ember hover:bg-ember/90 text-white font-body font-medium px-5 py-2.5 rounded-md transition-colors">`

Established page patterns (from services.astro, about.astro):
- Compact sapphire header banner: `<section class="bg-sapphire text-white py-12 md:py-16">` with centered heading
- Alternating section backgrounds: ivory (default) / sapphire-tint
- Design tokens: sapphire, gold, ember, charcoal, ivory, sapphire-tint, dark-bg, dark-surface, dark-text
- Font classes: font-heading (Montserrat), font-body (Inter)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env files and add Calendly assets to BaseLayout</name>
  <files>.env, .env.example, src/layouts/BaseLayout.astro</files>
  <action>
1. Create `.env` with placeholder values:
   ```
   PUBLIC_WEB3FORMS_KEY=YOUR_ACCESS_KEY_HERE
   PUBLIC_CALENDLY_URL=https://calendly.com/oriflect/discovery-call
   ```

2. Create `.env.example` with same structure (no real values) for version control.

3. Modify `src/layouts/BaseLayout.astro` -- add Calendly widget CSS and JS to the `<head>` section, after the favicon link and before the dark mode script:
   ```html
   <link href="https://assets.calendly.com/assets/external/widget.css" rel="stylesheet" />
   <script src="https://assets.calendly.com/assets/external/widget.js" type="text/javascript" async></script>
   ```

4. Also add a global `openCalendly()` helper function as an inline script in the `<head>` (after the Calendly script tag) that guards against the script not being loaded yet:
   ```html
   <script is:inline>
     function openCalendly() {
       if (typeof Calendly !== 'undefined') {
         Calendly.initPopupWidget({url: 'https://calendly.com/oriflect/discovery-call'});
       }
       return false;
     }
   </script>
   ```
   NOTE: Use the PUBLIC_CALENDLY_URL env var value. Since this is an is:inline script, the env var cannot be used directly -- hardcode the placeholder URL in the helper. The executor should use the same placeholder URL from .env. This is acceptable because the URL is configured once before launch.

5. Ensure `.gitignore` includes `.env` (check if it already does; if not, add it). Do NOT gitignore `.env.example`.
  </action>
  <verify>
    <automated>cd C:/Users/edwar/Desktop/Claude/test && cat .env && cat .env.example && grep -c "calendly" src/layouts/BaseLayout.astro && grep -c "openCalendly" src/layouts/BaseLayout.astro && npm run build</automated>
  </verify>
  <done>.env and .env.example exist with PUBLIC_WEB3FORMS_KEY and PUBLIC_CALENDLY_URL. BaseLayout.astro loads Calendly widget.css and widget.js in head. Global openCalendly() function defined. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Build contact page with form, Calendly embed, and FAQ</name>
  <files>src/pages/contact.astro</files>
  <action>
Create `src/pages/contact.astro` using BaseLayout. Structure:

**1. Compact sapphire header banner** (consistent with Services and About pages):
- Centered "Contact Us" heading with subtext like "Get in touch or book a discovery call"

**2. Side-by-side layout section** (ivory background):
- Use CSS grid: `grid-cols-1 lg:grid-cols-2 gap-12` with max-w-7xl container
- LEFT: Contact form
- RIGHT: Inline Calendly embed

**LEFT COLUMN -- Contact Form:**
- Heading: "Send Us a Message"
- Four fields: name (text, required), email (email, required), company (text, required), message (textarea, required)
- Each field: label, input with Tailwind styling matching design system (rounded-lg, border, focus:ring-2 focus:ring-sapphire), error paragraph hidden by default with `data-error="fieldname"`
- Hidden fields: `access_key` with value `{import.meta.env.PUBLIC_WEB3FORMS_KEY}`, `subject` with value "New Inquiry from Oriflect Website", honeypot `botcheck` checkbox (hidden with class="hidden" style="display: none;")
- Submit button: full-width, bg-ember styling, disabled by default, text "Send Message"
- Success state div (hidden by default, id="form-success"): green checkmark SVG, "Message Sent!" heading, "Thanks! We'll be in touch within 24 hours." paragraph

**RIGHT COLUMN -- Calendly Inline Embed:**
- Heading: "Book a Discovery Call"
- Brief text: "Schedule a free 30-minute call to discuss your AI needs."
- Container div (id="calendly-inline") with min-h-[650px] and rounded-lg border
- Inline script that calls `Calendly.initInlineWidget()` on window load with the placeholder URL and parentElement

**3. Contact details strip** (sapphire-tint background):
- Email (hello@oriflect.com), phone ((555) 123-4567), response time promise "We respond within 24 hours"
- Use flex/grid layout, inline SVG icons for email and phone

**4. FAQ section** (ivory background):
- Heading: "Frequently Asked Questions"
- 2-3 questions as simple heading + paragraph pairs (no accordion needed):
  - "What is a discovery call?" -- A free 30-minute conversation where we learn about your business, identify AI opportunities, and discuss whether we're a good fit. No commitment required.
  - "How long does an AI audit take?" -- Most audits are completed within 1-2 weeks, depending on the complexity of your operations and the number of processes we evaluate.
  - "Do I need technical expertise to work with Oriflect?" -- Not at all. We handle the technical side and explain everything in plain business terms. Our training programs ensure your team can confidently use any tools we implement.

**5. Inline validation script** (script is:inline at bottom):
- Track touched fields in a Set
- On blur: mark field as touched, validate, show/hide error with red border toggle
- On input: if touched, re-validate (live feedback after first blur)
- After each validation event, check all fields validity and enable/disable submit button
- Error messages: "Please enter your name.", "Please enter a valid email address.", "Please enter your company name.", "Please enter a message."

**6. Form submission script** (script is:inline at bottom):
- On form submit: preventDefault, disable button, change text to "Sending..."
- Collect FormData, convert to JSON with Object.fromEntries
- POST to https://api.web3forms.com/submit with Content-Type: application/json
- On success (data.success): hide form, show success div
- On error: re-enable button, restore text, log error to console

**Dark mode:** All form inputs get `dark:bg-dark-surface dark:border-dark-text/20 dark:text-dark-text`. Sections alternate correctly in dark mode.
  </action>
  <verify>
    <automated>cd C:/Users/edwar/Desktop/Claude/test && npm run build && grep -c "contact-form" src/pages/contact.astro && grep -c "calendly-inline" src/pages/contact.astro && grep -c "web3forms" src/pages/contact.astro && grep -c "data-error" src/pages/contact.astro</automated>
  </verify>
  <done>Contact page renders with: (1) sapphire header banner, (2) side-by-side form + Calendly embed that stacks on mobile, (3) contact details with response time promise, (4) FAQ section with 3 questions. Form has inline validation on blur, honeypot anti-spam, submit disabled until valid, and Web3Forms fetch submission with inline success message. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- contact.astro exists and contains form, Calendly inline embed, FAQ, contact details
- BaseLayout.astro loads Calendly widget.css and widget.js
- .env and .env.example contain PUBLIC_WEB3FORMS_KEY and PUBLIC_CALENDLY_URL
- Form has all 4 required fields (name, email, company, message) with validation
- Honeypot field present for anti-spam
- Success message replaces form on submission (no redirect)
</verification>

<success_criteria>
- Contact page builds and renders correctly
- Form collects name, email, company, message per CONT-01
- Inline Calendly embed visible on contact page per CONT-02
- Calendly assets loaded sitewide (prerequisite for plan 02 CTA wiring)
- .env configuration ready for user to add real API keys
</success_criteria>

<output>
After completion, create `.planning/phases/03-conversion-mechanisms/03-01-SUMMARY.md`
</output>
