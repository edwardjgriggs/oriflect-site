---
phase: 03-conversion-mechanisms
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/pages/index.astro
  - src/pages/services.astro
  - src/pages/about.astro
autonomous: false
requirements:
  - CONT-02

must_haves:
  truths:
    - "Clicking any 'Book a Discovery Call' CTA button opens Calendly popup (not page navigation)"
    - "'Contact' link appears in main navigation bar alongside Home, Services, About, Blog"
    - "'Contact' link appears in footer"
    - "All 9 existing href=#book links are converted to trigger Calendly popup"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Contact nav link + Calendly popup on CTA buttons"
      contains: "openCalendly"
    - path: "src/components/Footer.astro"
      provides: "Contact link in footer"
      contains: "/contact"
    - path: "src/pages/index.astro"
      provides: "Homepage CTAs trigger Calendly popup"
      contains: "openCalendly"
    - path: "src/pages/services.astro"
      provides: "Services CTAs trigger Calendly popup"
      contains: "openCalendly"
    - path: "src/pages/about.astro"
      provides: "About CTA triggers Calendly popup"
      contains: "openCalendly"
  key_links:
    - from: "src/components/Header.astro"
      to: "openCalendly()"
      via: "onclick on CTA buttons"
      pattern: "onclick.*openCalendly"
    - from: "src/pages/index.astro"
      to: "openCalendly()"
      via: "onclick on CTA links"
      pattern: "onclick.*openCalendly"
    - from: "src/pages/services.astro"
      to: "openCalendly()"
      via: "onclick on CTA links"
      pattern: "onclick.*openCalendly"
---

<objective>
Wire all existing CTA buttons sitewide to trigger Calendly popup and add "Contact" to navigation.

Purpose: Complete the conversion mechanism by making every "Book a Discovery Call" button functional and giving visitors a clear path to the contact page.
Output: All CTAs trigger Calendly popup, Contact link in nav and footer.
</objective>

<execution_context>
@C:/Users/edwar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/edwar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-conversion-mechanisms/03-CONTEXT.md
@.planning/phases/03-conversion-mechanisms/03-RESEARCH.md
@.planning/phases/03-conversion-mechanisms/03-01-SUMMARY.md

<interfaces>
<!-- The global openCalendly() function is defined in BaseLayout.astro (created in plan 01) -->
<!-- It guards against Calendly not being loaded: if (typeof Calendly !== 'undefined') Calendly.initPopupWidget({...}) -->
<!-- All CTA buttons should call onclick="openCalendly(); return false;" -->

CTA conversion pattern (from RESEARCH.md):
```html
<!-- BEFORE (current) -->
<a href="#book" class="bg-ember hover:bg-ember/90 text-white ...">
  Book a Discovery Call
</a>

<!-- AFTER -->
<a href="#book" onclick="openCalendly(); return false;" class="bg-ember hover:bg-ember/90 text-white ...">
  Book a Discovery Call
</a>
```
Keep href="#book" as no-JS fallback. The return false prevents navigation.

Existing CTA locations (9 total):
- Header.astro line 18: desktop CTA (href="#book")
- Header.astro line 29: mobile CTA (href="#book")
- index.astro line 21: hero CTA
- index.astro line 170: bottom CTA
- services.astro lines 120, 225, 329, 345: four service CTAs
- about.astro line 131: bottom CTA

Nav link pattern (from Header.astro):
```html
<a href="/contact" class="hover:text-gold transition-colors font-body font-medium">Contact</a>
```
Add after Blog link in desktop nav, mobile menu, and footer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Contact nav links and convert all CTAs to Calendly popup</name>
  <files>src/components/Header.astro, src/components/Footer.astro, src/pages/index.astro, src/pages/services.astro, src/pages/about.astro</files>
  <action>
**Header.astro modifications:**
1. Add "Contact" nav link in desktop nav (line ~16, after Blog link, before the CTA button):
   `<a href="/contact" class="hover:text-gold transition-colors font-body font-medium">Contact</a>`
2. Add "Contact" nav link in mobile menu (line ~52, after Blog link):
   `<a href="/contact" class="block py-3 hover:text-gold transition-colors font-body font-medium">Contact</a>`
3. Desktop CTA (line 17-22): add `onclick="openCalendly(); return false;"` to the existing anchor tag. Keep href="#book".
4. Mobile CTA (line 28-32): add `onclick="openCalendly(); return false;"` to the existing anchor tag. Keep href="#book".

**Footer.astro modifications:**
1. Add a "Quick Links" or "Pages" column, OR add a "Contact" link to the existing Contact column. Best approach: add to Contact column after the phone/location items:
   ```html
   <li><a href="/contact" class="hover:text-gold transition-colors">Contact Form</a></li>
   ```

**index.astro modifications:**
1. Hero CTA (line ~21): add `onclick="openCalendly(); return false;"` to the anchor tag
2. Bottom CTA (line ~170): add `onclick="openCalendly(); return false;"` to the anchor tag

**services.astro modifications:**
1. All 4 CTA links (lines ~120, ~225, ~329, ~345): add `onclick="openCalendly(); return false;"` to each anchor tag

**about.astro modifications:**
1. Bottom CTA (line ~131): add `onclick="openCalendly(); return false;"` to the anchor tag

IMPORTANT: Keep all existing href="#book" attributes as fallback. Only ADD the onclick handler. Do not change any other attributes, classes, or text content on these elements.
  </action>
  <verify>
    <automated>cd C:/Users/edwar/Desktop/Claude/test && npm run build && echo "--- Header contact link ---" && grep -c "/contact" src/components/Header.astro && echo "--- Footer contact link ---" && grep -c "/contact" src/components/Footer.astro && echo "--- openCalendly calls ---" && grep -rc "openCalendly" src/components/Header.astro src/pages/index.astro src/pages/services.astro src/pages/about.astro && echo "--- No remaining bare #book links ---" && grep -c 'href="#book"' src/components/Header.astro src/pages/index.astro src/pages/services.astro src/pages/about.astro | grep -v ":0"</automated>
  </verify>
  <done>All 9 CTA buttons have onclick="openCalendly(); return false;" added. "Contact" link appears in Header desktop nav, mobile menu, and Footer. Build succeeds. No CTA button navigates away -- all open Calendly popup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete conversion flow</name>
  <action>
    Human verification of the full conversion mechanism across the entire site.
  </action>
  <what-built>Complete conversion mechanism: contact page with form + Calendly embed, all CTA buttons wired to Calendly popup, Contact added to navigation.</what-built>
  <how-to-verify>
    1. Run `npm run preview` and open the site in a browser
    2. Verify "Contact" appears in the navigation bar (desktop and mobile)
    3. Click "Contact" -- should navigate to the contact page
    4. On the contact page, verify:
       a. Side-by-side layout: form on left, Calendly embed on right (stacked on mobile)
       b. Form has 4 fields: name, email, company, message
       c. Click into a field then click out without typing -- red border and error message should appear
       d. Fill all fields with valid data -- submit button should become enabled
       e. FAQ section visible below with 3 questions
       f. Contact details (email, phone) and "We respond within 24 hours" message visible
    5. Click "Book a Discovery Call" CTA in the header -- Calendly popup should open (may show error if placeholder URL, but popup mechanism should trigger)
    6. Check homepage, services, about page CTAs -- all should trigger Calendly popup
    7. Check footer -- "Contact" link should be present
    8. Test dark mode -- form and page should render correctly
  </how-to-verify>
  <verify>Visual and functional verification by user</verify>
  <done>User confirms all conversion mechanisms work correctly</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- All 9 CTA buttons call openCalendly() on click
- "Contact" link in Header (desktop + mobile) and Footer
- Contact page has form, Calendly embed, FAQ, contact details
- Form validation works on blur
- Calendly popup triggers from any page CTA
</verification>

<success_criteria>
- Every "Book a Discovery Call" button on the site opens Calendly popup (CONT-02 complete)
- Contact page accessible via navigation from any page
- Full conversion flow: visitor can either fill out form or book call from anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/03-conversion-mechanisms/03-02-SUMMARY.md`
</output>
